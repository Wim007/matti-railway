# Railway Environment Variables Template
# Copy these to Railway dashboard: Settings → Variables

# Database (automatically provided by Railway PostgreSQL service)
DATABASE_URL=postgresql://user:password@host:port/database

# OpenAI API
OPENAI_API_KEY=sk-proj-...
OPENAI_ASSISTANT_ID=asst_...

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-random-secret-here

# Analytics Dashboard (optional - for Matti Dashboard integration)
VITE_ANALYTICS_ENDPOINT=https://your-dashboard-url.com/api/analytics/event
mattisleutel=your-dashboard-api-key

# Node Environment
NODE_ENV=production

# Port (Railway sets this automatically)
PORT=3000
